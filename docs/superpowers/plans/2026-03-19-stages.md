# Stages Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan.

**Goal:** Add mandatory stages to workflows for GitLab-style horizontal pipeline visualization.

**Architecture:** Stages are a display-only concept — `depends_on` remains the execution model. Parser validates stages, graph API exposes them, frontend renders horizontal columns.

**Tech Stack:** Go (parser, server), Vue 3, TypeScript, Tailwind CSS

**Spec:** `docs/superpowers/specs/2026-03-19-stages-design.md`

---

## Chunk 1: Backend — Schema + Validation

### Task 1: Add Stage types to schema

**Files:**
- Modify: `internal/parser/schema.go`

- [ ] Add `Stage` struct with `Name` and `Description` fields
- [ ] Add `Stages []Stage` to `Workflow` struct
- [ ] Add `Stage string` to `Step` struct
- [ ] Commit

### Task 2: Add stage validation

**Files:**
- Modify: `internal/parser/parser.go`
- Modify: `internal/parser/parser_test.go`

- [ ] Add `validateStages` function:
  - At least one stage required
  - Stage names must be unique and non-empty
  - Every step must reference an existing stage
- [ ] Wire into `Validate` function
- [ ] Add tests: missing stages, duplicate names, step with unknown stage, step without stage
- [ ] Run tests to verify
- [ ] Commit

### Task 3: Add StageInfo to graph API

**Files:**
- Modify: `pkg/workflow/types.go`
- Modify: `internal/server/handlers.go`

- [ ] Add `StageInfo` type: `Name`, `Description`, `Steps []string`
- [ ] Add `Stages []StageInfo` to `Graph` struct
- [ ] Build stages list in `buildGraph` from workflow data
- [ ] Commit

---

## Chunk 2: Migrate Examples

### Task 4: Add stages to all 28 example workflows

**Files:**
- Modify: all `examples/*.yaml` files

- [ ] For each workflow, determine logical stages from the step structure
- [ ] Add `stages` block and `stage` field on every step
- [ ] Run `go test ./internal/parser/...` to verify all examples still parse
- [ ] Commit

---

## Chunk 3: Frontend — Types + StepTimeline

### Task 5: Add TypeScript types

**Files:**
- Modify: `web/frontend/src/composables/useWorkflowApi.ts`

- [ ] Add `StageInfo` interface: `name`, `description`, `steps: string[]`
- [ ] Add `stages` to `Graph` interface
- [ ] Commit

### Task 6: Rewrite StepTimeline as horizontal pipeline

**Files:**
- Rewrite: `web/frontend/src/components/StepTimeline.vue`

- [ ] Layout: flex horizontal, each stage is a column
- [ ] Stage header: name + description + duration + progress bar (green/amber/gray)
- [ ] Step cards: SVG status icon + name + label + action chip + duration
- [ ] Loop stages: amber left border + iteration badge
- [ ] Adaptive iteration display: dots ≤10, progress bar >10
- [ ] Conditional steps: purple `if` badge
- [ ] Running step: amber highlight + pulse
- [ ] Pending step: opacity 40% + dashed circle
- [ ] Click to expand: input/output/error/logs panel below card
- [ ] Live duration update (500ms timer)
- [ ] Commit

### Task 7: Rewrite WorkflowView

**Files:**
- Rewrite: `web/frontend/src/views/WorkflowView.vue`

- [ ] Same horizontal layout as StepTimeline but without execution status
- [ ] All steps show as neutral (dashed circle)
- [ ] Click navigates to step detail
- [ ] Commit

### Task 8: Wire stages in ExecutionView

**Files:**
- Modify: `web/frontend/src/views/ExecutionView.vue`

- [ ] Pass `graph.stages` to StepTimeline
- [ ] Commit

---

## Chunk 4: Build + Verify

### Task 9: Build and test

- [ ] `go build ./...`
- [ ] `go test ./internal/parser/...`
- [ ] `npx vite build`
- [ ] Manual test: run a workflow, check horizontal pipeline renders
- [ ] Manual test: check WorkflowView renders
- [ ] Manual test: check expand step shows input/output
- [ ] Commit build output
